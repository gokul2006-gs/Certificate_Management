# Vercel (frontend) — add as VITE_API_URL in Project Settings → Environment Variables.

# Must point to your Render backend. /api is required (code adds it if missing).
VITE_API_URL=https://certificate-management-2.onrender.com/api
